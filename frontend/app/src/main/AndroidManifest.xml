<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TakeTwo_PartB">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LuckySlotsActivity"/>
        <activity android:name=".ui.ChickenActivity"/>
        <activity android:name=".ui.DiceActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".ui.WheelActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".ui.CoinFlipActivity"/>
        <activity android:name=".ui.MainActivity"/>

    </application>

</manifest>